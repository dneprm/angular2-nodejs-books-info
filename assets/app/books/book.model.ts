export class Book {
    author: string;
    title: string;
    year: number;

    constructor(author: string, title: string, year: number) {
        this.author = author;
        this.title = title;
        this.year = year;
    }
}